[project]
name = "hangman"
version = "0.1.0"
description = "The CLI hangman game"
authors = [{ name = "Common Lisper", email = "atosdo@live.com" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = []


[tool.ruff]
line-length = 120

[dependency-groups]
dev = ["ruff>=0.8.6"]
